<div class="row clearfix">
	<div class="col-md-12 column">
		<div class="page-header">
			<h1>
				Signup for GitHub-Reminders <br> <small>Create reminders in your Issue Comments and get a scheduled notification</small>
			</h1>
		</div>
<!-- 		<p>
			Text 
		</p> -->
		<form class="form-horizontal" role="form", action="createuser", method="post">
			<div class="form-group">
				 <label for="fullname" class="col-sm-2 control-label">Name</label>
				<div class="col-sm-10">
					<input type="text" class="form-control" id="fullname", name="post[fullname]" />
				</div>
			</div>
			<div class="form-group">
				 <label for="email" class="col-sm-2 control-label">Email (Must be verified by GitHub)</label>
				<div class="col-sm-10">
					<select name="post[email]", id="email">
						<% @githubEmails.each do |e| %>
							<option value= "<%= "#{e}" %>" ><%= e %></option>
						<% end %>
					</select>
				</div>
			</div>
			<div class="form-group">
				 <label for="timezone" class="col-sm-2 control-label">Timezone (This is important for ensuring Reminders are scheduled for the correct Date and Time)</label>
				<div class="col-sm-10">
					<select name="post[timezone]", id="timezone">
						<% @timezonesList.each do |t| %>
							<option value= "<%= "#{t[0]} #{t[1]}" %>" ><%= "#{t[0]}  #{t[1]}" %></option>
						<% end %>
					</select>
				</div>
			</div>
			</div>
			<div class="form-group">
				<div class="col-sm-offset-2 col-sm-10">
					 <button type="submit" class="btn btn-default">Sign up</button>
				</div>
			</div>
		</form>
	</div>
</div>
